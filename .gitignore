# OS
.DS_Store

# Python / uv
__pycache__/
*.py[cod]
*$py.class
server/.venv/
.pytest_cache/
.ruff_cache/

# Local backend model weights (downloaded via `just download-model`, not committed)
server/models/

# Node / Vite
client/node_modules/
client/dist/

# Editors
.vscode/
.idea/
*.swp

# Agent guidance (personal, not shipped in repo)
.claude/
CLAUDE.md
AGENTS.md

# Project artifacts (to be confirmed once implemented)
*.bwbk

# Generated test fixtures — rebuilt by fixtures/seed_fixtures.py, not committed.
# Only the seed script is tracked; the .db files and docs are derived from it.
fixtures/*.db
fixtures/*.md

# UI iteration screenshots — generated on demand, not source
client/screenshots/
*.log
*.yml
*.png
TESTING_PLAYBOOK.md
fixtures/seed_fixtures.py
